name: 'xo-action'
description: 'GitHub Action ❤️ JavaScript happiness style linter'

inputs:
  name:
    description: ''
    required: false
    default: 'lint'

# outputs:

runs:
  using: 'docker'
  image: 'Dockerfile'

branding:
  icon: 'x'
  color: 'white'
